[project]
name = "token-server"
version = "0.1.0"
description = "FastAPI token server for LiveKit access token generation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.127.0",
    "livekit-api>=1.1.0",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.40.0",
]
